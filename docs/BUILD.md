# Build

Requires the .NET 8 SDK. The app is x64-only. See the README for full steps.

## Troubleshooting

If the chat box is missing, check the API name matches `Setting.lps`.

Build output is x64 only — pass `-p:Platform=x64`.
