# Configuration

Right-click Yumi → System → Settings → chat API → open settings.

- Protocol: Anthropic or OpenAI-compatible
- API base URL, key, model name

Config is stored in the `AIPet` line of `Setting.lps`.
