# Architecture

Host app (character + rendering) + AIPet brain plugin (chat, tools, memory).

The brain talks to your chosen LLM; the host gives it a body to express through.

Tools let the model play animations, save memory, and react to events.
